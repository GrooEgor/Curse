#ifndef OUTPUT_HPP
#define OUTPUT_HPP
void Print_Words(std::vector<std::string> document_data, std::string path);
void Save_to_file(
        std::vector<std::string> document_data, std::string path, int number);
#endif